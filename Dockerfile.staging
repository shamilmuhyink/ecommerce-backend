# --- Builder stage ---
FROM python:3.11.9-slim AS builder
WORKDIR /build

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc libpq-dev && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml .
RUN python -c \
    'import tomllib; \
    deps = tomllib.load(open("pyproject.toml", "rb"))["project"]["dependencies"]; \
    print("\n".join(deps))' \
    > requirements.txt \
    && pip install --no-cache-dir --upgrade pip \
    && pip install --no-cache-dir -r requirements.txt

# --- Staging stage ---
FROM python:3.11.9-slim AS staging
WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    libpq5 && rm -rf /var/lib/apt/lists/*

RUN groupadd --gid 1000 appgroup \
    && useradd --uid 1000 --gid appgroup --no-create-home appuser

COPY --from=builder /usr/local/lib/python3.11/site-packages /usr/local/lib/python3.11/site-packages
COPY --from=builder /usr/local/bin /usr/local/bin

COPY --chown=appuser:appgroup ./app ./app
COPY --chown=appuser:appgroup ./migrations ./migrations
COPY --chown=appuser:appgroup alembic.ini .
COPY --chown=appuser:appgroup Procfile.staging .

RUN mkdir -p /app/logs && chown -R appuser:appgroup /app/logs

# Install honcho for dual-process staging
RUN pip install --no-cache-dir honcho

USER appuser
EXPOSE 8000

HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"

# Run both uvicorn and celery using honcho
CMD ["honcho", "start", "-f", "Procfile.staging"]
